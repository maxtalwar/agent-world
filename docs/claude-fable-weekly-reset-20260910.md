# Fable weekly quota recovery — September 10, 2026

Run `web-claude-fable-5-1-22113c5f3d1f`, seed 41, remains at tick 12/60. Native Claude OAuth usage for the connector's saved Max 5x account reports session 46%, all-model weekly 55%, and model-scoped Fable weekly 100%. The exhausted Fable window resets at 2026-09-10 21:00 UTC; managed continuation is scheduled one minute later (14:01 PDT / 17:01 EDT). Subsecond reset values varied slightly between read-only queries; the saved recovery record carries the exact queried value. The user screenshot independently agrees to the minute.

The CLI supplied only “out of usage credits” with no reset, causing 300/600/1200/1800-second backoff inference checks. The portal incorrectly described these unknown-reset checks as “Continues”. Native usage exposes the relevant reset in `limits`, with `kind=weekly_scoped` and `scope.model.display_name=Fable`; legacy top-level weekly fields do not expose it.

Recovery stopped only this run's quota-sleeping worker and controller, archived all seed artifacts, added a provider-evidenced quota event, and updated operational checkpoint timing through the normal persistence writer. Managed resume retains source `9b1023075981d23f8af2dc5e99a73e5ac8512c90`, model, seed, completed world and execution identities. Agent state and usage/provider ledgers compare unchanged. No pending decision journal existed. Original artifacts and credential-free native evidence are in the job's `quota-recovery-20260910/` directory. Reserved and lifetime quota accounting retain previous waits and include the deadline extension. No simulation source or execution-environment migration was required. Fable 5 seed 11 was not touched.

Future-source repair reads the same CLI OAuth credential precedence/config directory only when scheduling a quota wait, selects the latest exhausted applicable model/global deadline, and preserves unknown reset state on unavailable or incomplete evidence. It does not invoke inference to discover a reset. Live portal static labels distinguish scheduled continuation from unknown-reset retry checks. Only the portal asset was deployed; the new resolver is committed for future launch-source integration, not injected into this pinned simulation.

Validation: 53 Python tests across native usage selection, session quota behavior, provider reset parsing and leaderboard; three JavaScript timing-label assertions; HTTP verification of the served asset; live managed supervisor and quota state at tick 12. No claim of post-reset advancement or benchmark completion.
