interface:
  display_name: "Set Up Agent World"
  short_description: "Prepare a clone and tune local worker limits"
  default_prompt: "Use $setup-agent-world to prepare this Agent World clone for reliable local runs."
