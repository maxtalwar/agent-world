# Opus 5 (1M) completion and admission review - 2026-09-12

Run `web-claude-opus-5-1m-680839180c53`, Participant v8-revised, medium, seeds 11 and 41, is admitted as replicated benchmark evidence. The maintained endpoint/winter projection scores capability 22.7, execution 90.7 and production 161.4; API-list equivalent cost averages $33.47/run and decision latency 17.27 seconds.

Both cells completed tick 60 with clean integrity, verified requested/returned model `claude-opus-5[1m]`, full usage coverage and complete self-declared transfer accounting. Seed 11 has 589 responses and usage records; seed 41 has 594. Each usage ledger has exactly one record per tick/agent decision. Both retained one quota wait/retry and no process resume; there are no discarded-attempt artifacts or repeated response decisions.

The pinned finalizer failed because canonical evidence paths lie outside its source checkout. Recovery called the existing current `finalize_job` with the canonical repository root, retaining pinned report generation and aggregation. Both worktrees are clean with empty exact diffs from launch commit `3a0867cfaf6c598ce506485a93fbcaa660ddab0c`. Manifest and report code fingerprints agree, and recipe digest remains `8de610d3cd050f86bf4fb5e16a04708e069d099435b41d2729b4717d9a0da006`. No source migration, altered conditions, acceptance waiver or owner exception was needed.

Full event streams (including their prefixes), usage, manifests, snapshots, checkpoints and reports were hash-identical across the verification finalization. Initial failed finalization preceded this hash capture; no claim of a pre-launch independent event backup is made. Readiness is ready with no blockers.

No batch or baseline handoff was supplied. The catalog keeps this recipe separate from historical Opus studies. These ordinary scores do not warrant a new institutional insight.

Validation: 17 finalization/acceptance tests passed; database integrity and foreign keys pass with 46 models, 105 runs and 47,211 usage records. Exact source checks, lifecycle events, raw scores and artifact hashes are in [the review JSON](opus5-1m-completion-20260912.json).
