interface:
  display_name: "Analyze Agent World Benchmarks"
  short_description: "Analyze Agent World benchmark evidence"
  default_prompt: "Use $analyze-agent-world-benchmarks to compare the Agent World benchmark results with certification and provenance checks."
